# ===================================================================
# DATASOURCE - Oracle Cloud / Autonomous Database
# ===================================================================
# Ajuste o ORACLE_DATABASE_PROFILE para high, medium, low, tp ou tpurgent conforme o serviço configurado.
# Ajuste a env var TNS_ADMIN se o wallet estiver em outro diretório.

spring.datasource.url=jdbc:oracle:thin:@vortekradaresbd_${ORACLE_DATABASE_PROFILE:tpurgent}?TNS_ADMIN=C:/Users/thiago/Documents/Git/API4/cimob-back/cimob/src/main/resources/wallet
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ADMIN}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:V@rtek_2025_}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Pool de conexões (HikariCP)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-test-query=SELECT 1 FROM dual

# ===================================================================
# JPA / Hibernate
# ===================================================================
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Mantém nomes exatos das colunas e tabelas (sem converter camelCase → snake_case)
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ===================================================================
# JWT
# ===================================================================
jwt.secret=a9F!7bR2vQ$kLp8zXw3@eT6yU1mNc4Gh
jwt.expiration=36000000
jwt.refresh-expiration=604800000


# BOT TELEGRAM
telegram.bot.token=${TELEGRAM_BOT_TOKEN:{BOT_TOKEN}}
telegram.chat.id=${TELEGRAM_CHAT_ID:{CHAT_ID}}
